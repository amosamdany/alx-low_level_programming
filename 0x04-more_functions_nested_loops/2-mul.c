#include <stdio.h>
#include "main.h"
/**
 * mul - a function that multiplies the input a,b
 * @a: one variable input
 * @b: two variable input
 * Return: mul returns the multiplication
 */

int mul(int a, int b)
{
int mul = a * b;
return (mul);
}
